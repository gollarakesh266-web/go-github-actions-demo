name: ci workflow
on: 
  push:
    branches:
     - main

jobs:
  build:
    strategy:
      matrix:
        go-version: ["1.23","1.24.6"]
    uses: ./.github/workflows/reusable-go-build.yaml
    with: 
      go-version: ${{ matrix.go-version }}
